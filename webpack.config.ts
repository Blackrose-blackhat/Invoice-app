// webpack.config.js
// eslint-disable-next-line @typescript-eslint/no-var-requires
const path = require('path');

module.exports = {
  target: 'electron-main', // or 'electron-renderer' for renderer process
  // other Webpack configurations...
};
